# Assets

The baseline task uses only PsychoPy text, textbox, and rectangle primitives.
Chinese category-exemplar materials are defined in `config/*.yaml`; no external
participant-facing media or temporary stand-in assets are required.
